export type UserData = {
    id: string;
    name: string;
    avatar: string;
    isPro: boolean;
    email: string;
    accessToken: string;
};

export type AuthData = {
    email: string;
    password: string;
}