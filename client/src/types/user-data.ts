export type UserData = {
    name: string;
    avatarUrl: string;
    isPro: boolean;
    email: string;
    accessToken: string;
};

export type AuthData = {
    email: string;
    password: string;
}